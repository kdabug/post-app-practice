import "./style.css";
import React from "react";

const Tweet = props => {
  const { tweetData } = props;
  return (
    <div className="c-card__container">
      <h1>${tweedData.text}</h1>
    </div>
  );
};
export default Tweet;
